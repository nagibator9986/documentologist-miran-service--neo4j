---
phase: 05-production-hardening
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - documentologist-miran-service--neo4j/langgraph-agent/docker-compose.yml
  - documentologist-miran-service--neo4j/langgraph-agent/docker-compose.prod.yml
  - documentologist-miran-service--neo4j/langgraph-agent/METRICS-V1.md
autonomous: true
gap_closure: true
requirements: [FR-7, NFR-2]

must_haves:
  truths:
    - "langgraph-agent service has a Docker healthcheck pointing to /health"
    - "NFR-2 latency targets (P95 thresholds) are documented in METRICS-V1.md as pass/fail criteria"
  artifacts:
    - path: "documentologist-miran-service--neo4j/langgraph-agent/docker-compose.yml"
      provides: "healthcheck block for langgraph-agent service"
      contains: "healthcheck"
    - path: "documentologist-miran-service--neo4j/langgraph-agent/METRICS-V1.md"
      provides: "NFR-2 latency targets table"
      contains: "P95"
  key_links:
    - from: "docker-compose.yml langgraph-agent healthcheck"
      to: "/health endpoint in app/main.py"
      via: "curl -f http://localhost:8001/health"
      pattern: "curl.*localhost:8001/health"
---

<objective>
Close two verification gaps from 05-VERIFICATION.md:

1. **Gap 1 (FR-7):** Add Docker healthcheck to `langgraph-agent` service in docker-compose.yml. The /health endpoint already exists and works — it just needs to be wired into Docker's native health monitoring. Every other service (qdrant, neo4j, redis, postgres, mlflow, minio) already has a healthcheck; langgraph-agent is the only one missing it.

2. **Gap 2 (NFR-2):** Add concrete latency pass/fail thresholds to METRICS-V1.md. The eval runner already captures p95_elapsed_s, but the document does not state the numeric targets from REQUIREMENTS.md (search/analyze/verify P95 < 30s, generate P95 < 90s, supervisor P95 < 2s).

Purpose: Complete FR-7 and NFR-2 compliance for Phase 5 verification.
Output: Updated docker-compose.yml, docker-compose.prod.yml, METRICS-V1.md
</objective>

<execution_context>
@C:/Users/tleub/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/tleub/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-production-hardening/05-VERIFICATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Docker healthcheck to langgraph-agent service</name>
  <files>documentologist-miran-service--neo4j/langgraph-agent/docker-compose.yml, documentologist-miran-service--neo4j/langgraph-agent/docker-compose.prod.yml</files>
  <read_first>
    - documentologist-miran-service--neo4j/langgraph-agent/docker-compose.yml
    - documentologist-miran-service--neo4j/langgraph-agent/docker-compose.prod.yml
    - documentologist-miran-service--neo4j/langgraph-agent/app/main.py (lines around /health endpoint, ~line 117)
  </read_first>
  <action>
Add a healthcheck block to the `langgraph-agent` service in `docker-compose.yml` (base file). Insert it after the `volumes` key of the langgraph-agent service block (around line 75). Use the same pattern as other services in the file:

```yaml
    healthcheck:
      test: ["CMD-SHELL", "curl -sf http://localhost:8001/health || exit 1"]
      interval: 15s
      timeout: 5s
      retries: 5
      start_period: 30s
```

Key values:
- Port 8001 (hardcoded in the service, not variable)
- `/health` endpoint (liveness probe, already exists in app/main.py ~line 117)
- `start_period: 30s` — longer than infrastructure services because the app needs to initialize FastAPI + load config
- `interval: 15s` — same as neo4j/mlflow (not too aggressive for an app service)
- `retries: 5` — same as mlflow

Also update the `depends_on` conditions for langgraph-agent in `docker-compose.prod.yml` if needed. Since the prod overlay does not redefine depends_on, the base healthcheck will be inherited automatically — no prod overlay change needed for the healthcheck itself. However, the prod overlay should NOT override the healthcheck (verify it does not).

Do NOT change any other service definitions. Only add the healthcheck block to langgraph-agent.
  </action>
  <verify>
    <automated>cd documentologist-miran-service--neo4j/langgraph-agent && grep -A 5 "healthcheck" docker-compose.yml | grep -c "localhost:8001/health"</automated>
  </verify>
  <acceptance_criteria>
    - grep "healthcheck" docker-compose.yml returns a match inside the langgraph-agent service block
    - grep "curl.*localhost:8001/health" docker-compose.yml returns exactly 1 match
    - grep "start_period" docker-compose.yml returns a match for the langgraph-agent healthcheck
    - No other services were modified (diff shows only langgraph-agent block changed)
  </acceptance_criteria>
  <done>langgraph-agent service in docker-compose.yml has a healthcheck block using curl to /health endpoint with interval/timeout/retries/start_period, matching the pattern of other services in the file</done>
</task>

<task type="auto">
  <name>Task 2: Add NFR-2 latency targets to METRICS-V1.md</name>
  <files>documentologist-miran-service--neo4j/langgraph-agent/METRICS-V1.md</files>
  <read_first>
    - documentologist-miran-service--neo4j/langgraph-agent/METRICS-V1.md
    - .planning/REQUIREMENTS.md (NFR-2 section)
  </read_first>
  <action>
Add a new section to METRICS-V1.md titled "## Latency Targets (NFR-2)" immediately after the "## Target Thresholds (Milestone 1)" table. Insert the following content:

```markdown
## Latency Targets (NFR-2)

Per-agent P95 latency pass/fail thresholds measured by `p95_elapsed_s` in eval output:

| Agent / Task | P95 Target | How Measured |
|-------------|------------|--------------|
| search | < 30s | `p95_elapsed_s` for category=routing queries with expected_intent=search |
| analyze (qa/compare/extract/summary) | < 30s | `p95_elapsed_s` for category=routing queries with expected_intent=analyze |
| verify | < 30s | `p95_elapsed_s` for category=json_validity queries with expected_intent=verify |
| generate | < 90s | `p95_elapsed_s` for category=json_validity queries with expected_intent=generate |
| supervisor classification | < 2s | Time from request receipt to intent determination (logged as supervisor elapsed) |

**Note:** These thresholds assume qwen2.5:14b running locally via Ollama. Larger models
or remote inference will have different latency profiles. Adjust targets accordingly.
```

Do NOT modify any other section of the file. Insert only after the first table, before "## How to Run".
  </action>
  <verify>
    <automated>cd documentologist-miran-service--neo4j/langgraph-agent && grep -c "NFR-2" METRICS-V1.md && grep -c "< 30s" METRICS-V1.md && grep -c "< 90s" METRICS-V1.md && grep -c "< 2s" METRICS-V1.md</automated>
  </verify>
  <acceptance_criteria>
    - grep "NFR-2" METRICS-V1.md returns >= 1 match
    - grep "< 30s" METRICS-V1.md returns >= 1 match (search/analyze/verify targets)
    - grep "< 90s" METRICS-V1.md returns >= 1 match (generate target)
    - grep "< 2s" METRICS-V1.md returns >= 1 match (supervisor target)
    - grep "Latency Targets" METRICS-V1.md returns 1 match
    - The existing "Target Thresholds" and "Recorded Runs" tables are unchanged
  </acceptance_criteria>
  <done>METRICS-V1.md contains a "Latency Targets (NFR-2)" section with concrete P95 pass/fail thresholds for all agent types matching REQUIREMENTS.md NFR-2 values</done>
</task>

</tasks>

<verification>
1. `grep -c "healthcheck" documentologist-miran-service--neo4j/langgraph-agent/docker-compose.yml` returns 7 (was 6, +1 for langgraph-agent)
2. `grep "NFR-2" documentologist-miran-service--neo4j/langgraph-agent/METRICS-V1.md` returns match
3. `grep "< 30s" documentologist-miran-service--neo4j/langgraph-agent/METRICS-V1.md` returns match
4. All existing content in both files remains intact
</verification>

<success_criteria>
- FR-7 fully satisfied: every service in docker-compose.yml (including langgraph-agent) has a healthcheck
- NFR-2 fully satisfied: METRICS-V1.md documents concrete P95 latency pass/fail thresholds per agent type
- Both gaps from 05-VERIFICATION.md are closed
- No regressions to existing configuration or documentation
</success_criteria>

<output>
After completion, create `.planning/phases/05-production-hardening/05-04-SUMMARY.md`
</output>
