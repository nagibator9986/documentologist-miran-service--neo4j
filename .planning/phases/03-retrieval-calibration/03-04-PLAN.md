---
phase: 03-retrieval-calibration
plan: 04
type: execute
wave: 3
depends_on: [03-01, 03-02, 03-03]
files_modified:
  - documentologist-miran-service--neo4j/langgraph-agent/tests/eval/dataset.json
autonomous: true
requirements: [FR-2]

must_haves:
  truths:
    - "10 new probing entries exist in dataset.json with category=retrieval"
    - "Each probing entry has intent=search to route through search_agent"
    - "expected_docs are filled with actual indexed filenames, not placeholders"
    - "Eval runner completes without errors and shows retrieval_recall_at_5"
  artifacts:
    - path: "documentologist-miran-service--neo4j/langgraph-agent/tests/eval/dataset.json"
      provides: "Extended eval dataset with 10 probing queries"
      contains: "probe-01"
  key_links:
    - from: "tests/eval/dataset.json probe entries"
      to: "tests/eval/run_eval.py"
      via: "category=retrieval filter"
      pattern: '"category": "retrieval"'
---

<objective>
Add 10 probing retrieval entries to the eval dataset covering core banking/legal topics and validate retrieval_recall_at_5 >= 0.80.

Purpose: After threshold calibration and query expansion removal, validate that the system actually finds indexed documents for direct questions about their content.
Output: dataset.json with 10 new probe entries, eval run showing recall >= 0.80.
</objective>

<execution_context>
@C:/Users/tleub/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/tleub/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-retrieval-calibration/03-CONTEXT.md
@.planning/phases/03-retrieval-calibration/03-RESEARCH.md
@.planning/phases/03-retrieval-calibration/03-01-SUMMARY.md
@.planning/phases/03-retrieval-calibration/03-02-SUMMARY.md
@.planning/phases/03-retrieval-calibration/03-03-SUMMARY.md

<interfaces>
<!-- Existing eval entry schema (from dataset.json) -->
{
  "id": "retrieval-01",
  "query": "...",
  "expected_intent": "search",
  "expected_docs": ["zakon_o_bankah.pdf", "trebovaniya_k_kapitalu.pdf"],
  "expected_json_valid": null,
  "category": "retrieval",
  "tags": ["search", "retrieval", "..."]
}

<!-- Eval runner retrieval check (run_eval.py):
     Checks if ANY filename in expected_docs appears as substring in citations array.
     Category filter: entry.get("category") == "retrieval"
     Metric: retrieval_recall_at_5
-->

<!-- Existing retrieval entry IDs: retrieval-01 through retrieval-08 -->
<!-- New IDs must be: probe-01 through probe-10 (no collision) -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Discover actual indexed filenames and add 10 probing entries to dataset.json</name>
  <files>documentologist-miran-service--neo4j/langgraph-agent/tests/eval/dataset.json</files>
  <read_first>
    documentologist-miran-service--neo4j/langgraph-agent/tests/eval/dataset.json,
    documentologist-miran-service--neo4j/langgraph-agent/tests/eval/run_eval.py
  </read_first>
  <action>
STEP 1: Discover actual indexed filenames.
Run `GET /api/v1/debug/retrieval?q=банк` (or check if there is a documents list endpoint) to see what filenames are actually in the `bank_knowledge` collection. If the API is not running, check the existing retrieval entries (retrieval-01 through retrieval-08) for the filenames they use as a reference: `zakon_o_bankah.pdf`, `trebovaniya_k_kapitalu.pdf`, `licenzirovanie.pdf`, `trebovaniya_uchrediteli.pdf`, `aml_zakon.pdf`, `likvidaciya.pdf`, `depozity.pdf`, `bankovskie_operacii.pdf`, `prudencialnye_normativy.pdf`.

Use these same filenames (already used by existing entries) in the new probing entries. If API is available and returns different filenames, use those instead.

STEP 2: Add 10 new entries to dataset.json array (append before closing `]`).
IDs: `probe-01` through `probe-10`. All must have:
- `"expected_intent": "search"` (routes to search agent which returns citations)
- `"category": "retrieval"` (exact string — eval runner filters on this)
- `"expected_json_valid": null` (search returns free text, not JSON)
- `"tags"` array including `"search"`, `"retrieval"`, `"probe"`

The 10 queries and their likely matching docs (adjust based on actual indexed filenames):

```json
{"id": "probe-01", "query": "порядок открытия депозитного вклада физическим лицом", "expected_intent": "search", "expected_docs": ["PLACEHOLDER"], "expected_json_valid": null, "category": "retrieval", "tags": ["search", "retrieval", "deposits", "probe"]},
{"id": "probe-02", "query": "ставка рефинансирования Национального Банка", "expected_intent": "search", "expected_docs": ["PLACEHOLDER"], "expected_json_valid": null, "category": "retrieval", "tags": ["search", "retrieval", "rates", "probe"]},
{"id": "probe-03", "query": "требования к заёмщику при получении кредита", "expected_intent": "search", "expected_docs": ["PLACEHOLDER"], "expected_json_valid": null, "category": "retrieval", "tags": ["search", "retrieval", "credit", "probe"]},
{"id": "probe-04", "query": "залог имущества как обеспечение кредита", "expected_intent": "search", "expected_docs": ["PLACEHOLDER"], "expected_json_valid": null, "category": "retrieval", "tags": ["search", "retrieval", "collateral", "probe"]},
{"id": "probe-05", "query": "условия кредитования юридических лиц", "expected_intent": "search", "expected_docs": ["PLACEHOLDER"], "expected_json_valid": null, "category": "retrieval", "tags": ["search", "retrieval", "credit", "probe"]},
{"id": "probe-06", "query": "процентная ставка по займу в банке", "expected_intent": "search", "expected_docs": ["PLACEHOLDER"], "expected_json_valid": null, "category": "retrieval", "tags": ["search", "retrieval", "rates", "probe"]},
{"id": "probe-07", "query": "права вкладчика при банкротстве банка", "expected_intent": "search", "expected_docs": ["PLACEHOLDER"], "expected_json_valid": null, "category": "retrieval", "tags": ["search", "retrieval", "deposits", "probe"]},
{"id": "probe-08", "query": "обязанности банка перед вкладчиком", "expected_intent": "search", "expected_docs": ["PLACEHOLDER"], "expected_json_valid": null, "category": "retrieval", "tags": ["search", "retrieval", "obligations", "probe"]},
{"id": "probe-09", "query": "страхование депозитов в Казахстане", "expected_intent": "search", "expected_docs": ["PLACEHOLDER"], "expected_json_valid": null, "category": "retrieval", "tags": ["search", "retrieval", "insurance", "probe"]},
{"id": "probe-10", "query": "порядок погашения кредита досрочно", "expected_intent": "search", "expected_docs": ["PLACEHOLDER"], "expected_json_valid": null, "category": "retrieval", "tags": ["search", "retrieval", "credit", "probe"]}
```

Replace `PLACEHOLDER` with actual filenames from the indexed collection. Each entry should have 1-2 expected doc filenames that are most likely to contain relevant content.

If you cannot determine actual filenames (API not running), use empty `expected_docs: []` — this means those entries will be excluded from recall calculation (they still validate routing).

STEP 3: Validate JSON structure.
Run `python -c "import json; data = json.load(open('tests/eval/dataset.json')); probes = [e for e in data if e['id'].startswith('probe-')]; assert len(probes) == 10, f'got {len(probes)}'; assert all(e['category'] == 'retrieval' for e in probes); print(f'OK: {len(probes)} probe entries')"` to confirm.
  </action>
  <verify>
    <automated>cd documentologist-miran-service--neo4j/langgraph-agent && python -c "import json; data = json.load(open('tests/eval/dataset.json')); probes = [e for e in data if e['id'].startswith('probe-')]; assert len(probes) == 10, f'got {len(probes)}'; assert all(e['category'] == 'retrieval' for e in probes); assert all(e['expected_intent'] == 'search' for e in probes); print(f'OK: {len(probes)} probe entries, all retrieval category, all search intent')"</automated>
  </verify>
  <acceptance_criteria>
    - `python -c "import json; d=json.load(open('tests/eval/dataset.json')); print(len(d))"` shows 41 (31 existing + 10 new)
    - `grep -c '"probe-"' tests/eval/dataset.json` returns 10
    - `grep -c '"category": "retrieval"' tests/eval/dataset.json` returns 18 (8 existing + 10 new)
    - All 10 probe entries have `"expected_intent": "search"`
    - All 10 probe entries have `"category": "retrieval"` (exact string)
    - JSON is valid: `python -c "import json; json.load(open('tests/eval/dataset.json'))"` succeeds
    - No duplicate IDs in the dataset
  </acceptance_criteria>
  <done>10 probe entries (probe-01 through probe-10) added to dataset.json, all with category=retrieval, intent=search, expected_docs filled with best-available filenames, valid JSON</done>
</task>

<task type="auto">
  <name>Task 2: Run eval suite and verify retrieval_recall_at_5 >= 0.80</name>
  <files></files>
  <read_first>
    documentologist-miran-service--neo4j/langgraph-agent/tests/eval/run_eval.py
  </read_first>
  <action>
Run the full eval suite from the langgraph-agent directory:
```bash
cd documentologist-miran-service--neo4j/langgraph-agent
python tests/eval/run_eval.py --timeout 120
```

Check the output for:
1. `retrieval_recall_at_5` must be >= 0.80
2. `routing_accuracy` should not regress (baseline from Phase 1)
3. No crashes or unhandled exceptions

If the eval runner requires a running API server, note this and document the command to run once the server is up. The verify step is the UAT gate for this phase.

If retrieval_recall_at_5 < 0.80 and the system is running, investigate which entries are failing (the runner should print per-entry results) and check whether expected_docs filenames match actual indexed document names.
  </action>
  <verify>
    <automated>cd documentologist-miran-service--neo4j/langgraph-agent && python tests/eval/run_eval.py --timeout 120 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `python tests/eval/run_eval.py` completes without Python exceptions
    - Output includes `retrieval_recall_at_5` metric
    - `retrieval_recall_at_5 >= 0.80` (phase gate)
    - `routing_accuracy` shows no regression vs Phase 1 baseline
  </acceptance_criteria>
  <done>Eval suite runs, retrieval_recall_at_5 >= 0.80, no regression in routing_accuracy or json_validity_rate</done>
</task>

</tasks>

<verification>
- `python -c "import json; d=json.load(open('tests/eval/dataset.json')); print(len(d))"` — 41 entries
- `python tests/eval/run_eval.py --category retrieval --timeout 60` — retrieval_recall_at_5 >= 0.80
- `python tests/eval/run_eval.py --timeout 120` — full suite green, no regression
</verification>

<success_criteria>
- dataset.json has 10 new probe entries (probe-01 to probe-10) with category=retrieval
- All probing entries use actual indexed filenames in expected_docs (not placeholders)
- Eval suite reports retrieval_recall_at_5 >= 0.80
- No regression in routing_accuracy or json_validity_rate
</success_criteria>

<output>
After completion, create `.planning/phases/03-retrieval-calibration/03-04-SUMMARY.md`
</output>
