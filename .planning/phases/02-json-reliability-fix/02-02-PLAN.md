---
phase: 02-json-reliability-fix
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - documentologist-miran-service--neo4j/langgraph-agent/app/prompts/__init__.py
autonomous: true
requirements: [FR-1]
must_haves:
  truths:
    - "All four JSON system prompts contain explicit anti-markdown instructions in Russian"
    - "Prompts instruct the model to return ONLY raw JSON starting with { and ending with }"
    - "Prompts instruct to never wrap in backticks or markdown code blocks"
    - "Existing prompt content is preserved — only anti-markdown block is added"
  artifacts:
    - path: "documentologist-miran-service--neo4j/langgraph-agent/app/prompts/__init__.py"
      provides: "Updated JSON prompts with anti-markdown instructions"
      contains: "НЕ оборачивай в"
  key_links:
    - from: "app/prompts/__init__.py"
      to: "app/agents/verify_agent.py"
      via: "VERIFY_COMPLIANCE imported in verify_agent"
      pattern: "from.*prompts.*import.*VERIFY_COMPLIANCE"
    - from: "app/prompts/__init__.py"
      to: "app/agents/generate_agent.py"
      via: "GENERATE_PLAN imported in generate_agent"
      pattern: "from.*prompts.*import.*GENERATE_PLAN"
    - from: "app/prompts/__init__.py"
      to: "app/agents/analyze_agent.py"
      via: "ANALYZE_COMPARE, ANALYZE_EXTRACT imported in analyze_agent"
      pattern: "from.*prompts.*import.*ANALYZE_COMPARE"
---

<objective>
Add explicit anti-markdown instructions to all four JSON-producing system prompts (VERIFY_COMPLIANCE, GENERATE_PLAN, ANALYZE_COMPARE, ANALYZE_EXTRACT).

Purpose: Even with schema-constrained generation, prompt-level guidance is defense-in-depth. Currently none of the four prompts tell the model to avoid backticks or markdown wrapping.
Output: Updated `app/prompts/__init__.py` with anti-markdown blocks in all JSON prompts.
</objective>

<execution_context>
@C:/Users/tleub/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/tleub/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-json-reliability-fix/02-RESEARCH.md

<interfaces>
<!-- Current prompts that need modification -->

From app/prompts/__init__.py — 4 prompts ending with "Выдай JSON:" + example structure:
- VERIFY_COMPLIANCE (line 66): ends with JSON example at line 86
- GENERATE_PLAN (line 90): ends with JSON example at line 109
- ANALYZE_COMPARE (line 135): ends with JSON example at line 150
- ANALYZE_EXTRACT (line 163): ends with JSON example at line 179

None have anti-markdown instructions. All say "Выдай JSON:" but don't say "no backticks".
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add anti-markdown instructions to all four JSON prompts</name>
  <files>documentologist-miran-service--neo4j/langgraph-agent/app/prompts/__init__.py</files>
  <read_first>
    - documentologist-miran-service--neo4j/langgraph-agent/app/prompts/__init__.py
  </read_first>
  <action>
For each of the four JSON system prompts (VERIFY_COMPLIANCE, GENERATE_PLAN, ANALYZE_COMPARE, ANALYZE_EXTRACT), add the following anti-markdown instruction block IMMEDIATELY BEFORE the "Выдай JSON:" line.

The block to insert (identical for all four prompts):
```
ФОРМАТ ОТВЕТА:
Верни ТОЛЬКО чистый JSON объект.
НЕ оборачивай в ```json``` или другие markdown-блоки.
НЕ добавляй текст до или после JSON.
Ответ должен начинаться с { и заканчиваться на }.
```

Specifically:

1. **VERIFY_COMPLIANCE** — insert the block before "Выдай JSON:" (currently around line 79). The prompt currently has rules 1-4, so add a blank line after rule 4, then the anti-markdown block, then "Выдай JSON:".

2. **GENERATE_PLAN** — insert the block before "Выдай JSON:" (currently around line 96). The prompt currently says "Для стороны 1..." then goes to "Выдай JSON:". Add blank line, anti-markdown block, then "Выдай JSON:".

3. **ANALYZE_COMPARE** — insert the block before "Выдай JSON:" (currently around line 142). The prompt has the compare instructions, then "Выдай JSON:". Add blank line, anti-markdown block, then "Выдай JSON:".

4. **ANALYZE_EXTRACT** — insert the block before "Выдай JSON:" (currently around line 169). The prompt has extract instructions, then "Выдай JSON:". Add blank line, anti-markdown block, then "Выдай JSON:".

Do NOT modify:
- SUPERVISOR_CLASSIFY (not JSON output)
- SEARCH_EXPERT (not JSON output)
- SEARCH_EXPAND_QUERY (not JSON output)
- ANALYZE_DOCUMENT (qa task — will be handled if needed later)
- ANALYZE_QA (free text, not JSON)
- ANALYZE_SUMMARY (free text, not JSON)
- GENERATE_VALIDATE (free text output)

Preserve all existing prompt text exactly. Only insert the anti-markdown block.
  </action>
  <verify>
    <automated>cd documentologist-miran-service--neo4j/langgraph-agent && python -c "
from app.prompts import VERIFY_COMPLIANCE, GENERATE_PLAN, ANALYZE_COMPARE, ANALYZE_EXTRACT
anti = 'НЕ оборачивай в'
for name, prompt in [('VERIFY_COMPLIANCE', VERIFY_COMPLIANCE), ('GENERATE_PLAN', GENERATE_PLAN), ('ANALYZE_COMPARE', ANALYZE_COMPARE), ('ANALYZE_EXTRACT', ANALYZE_EXTRACT)]:
    assert anti in prompt, f'{name} missing anti-markdown instruction'
    assert 'Выдай JSON:' in prompt, f'{name} missing JSON example'
    print(f'{name}: anti-markdown OK')
print('All 4 prompts updated')
"</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "НЕ оборачивай в" app/prompts/__init__.py returns 4 (one per prompt)
    - grep -c "Ответ должен начинаться с" app/prompts/__init__.py returns 4
    - grep -c "ФОРМАТ ОТВЕТА:" app/prompts/__init__.py returns 4
    - grep -q "Выдай JSON:" app/prompts/__init__.py (still present, not deleted)
    - SUPERVISOR_CLASSIFY does NOT contain "НЕ оборачивай"
    - SEARCH_EXPERT does NOT contain "НЕ оборачивай"
    - ANALYZE_QA does NOT contain "НЕ оборачивай"
  </acceptance_criteria>
  <done>All four JSON prompts (VERIFY_COMPLIANCE, GENERATE_PLAN, ANALYZE_COMPARE, ANALYZE_EXTRACT) contain the anti-markdown instruction block in Russian. Non-JSON prompts are untouched.</done>
</task>

</tasks>

<verification>
```bash
cd documentologist-miran-service--neo4j/langgraph-agent && python -c "
from app.prompts import (
    VERIFY_COMPLIANCE, GENERATE_PLAN, ANALYZE_COMPARE, ANALYZE_EXTRACT,
    SUPERVISOR_CLASSIFY, SEARCH_EXPERT, ANALYZE_QA, ANALYZE_SUMMARY
)
# JSON prompts have anti-markdown
for p in [VERIFY_COMPLIANCE, GENERATE_PLAN, ANALYZE_COMPARE, ANALYZE_EXTRACT]:
    assert 'НЕ оборачивай' in p
# Non-JSON prompts do NOT have anti-markdown
for p in [SUPERVISOR_CLASSIFY, SEARCH_EXPERT, ANALYZE_QA, ANALYZE_SUMMARY]:
    assert 'НЕ оборачивай' not in p
print('Verification passed')
"
```
</verification>

<success_criteria>
- Exactly 4 prompts contain the anti-markdown block
- No non-JSON prompts were modified
- All existing prompt content is preserved
- The anti-markdown block is in Russian matching the prompt language
</success_criteria>

<output>
After completion, create `.planning/phases/02-json-reliability-fix/02-02-SUMMARY.md`
</output>
