---
phase: 04-agent-logic-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - documentologist-miran-service--neo4j/langgraph-agent/app/agents/supervisor.py
  - documentologist-miran-service--neo4j/langgraph-agent/tests/eval/dataset.json
autonomous: true
requirements: [P4-ROUTE, P4-EVAL]

must_haves:
  truths:
    - "Queries like 'верно ли что...' route to verify via keyword tier, not LLM fallback"
    - "Queries like 'условия кредитования...' route to search via keyword tier"
    - "Queries like 'проанализируй документ' route to analyze via keyword tier"
    - "All three supervisor log lines use identical format: supervisor: tier=%s intent=%s query=%r"
    - "Eval dataset contains routing entries for the newly added keyword patterns"
  artifacts:
    - path: "documentologist-miran-service--neo4j/langgraph-agent/app/agents/supervisor.py"
      provides: "Expanded keyword patterns + normalized logging"
      contains: "верно\\s+ли"
    - path: "documentologist-miran-service--neo4j/langgraph-agent/tests/eval/dataset.json"
      provides: "New routing eval entries for edge cases"
  key_links:
    - from: "supervisor.py _VERIFY_KW"
      to: "tests/eval/dataset.json routing entries"
      via: "patterns match eval query text"
      pattern: "верно\\s+ли|допустим"
---

<objective>
Expand supervisor keyword patterns to cover banking/legal edge cases that currently fall to LLM tier unnecessarily, normalize logging format across all three tiers, and add eval entries to validate the new patterns.

Purpose: Reduce LLM fallback calls (cheaper routing, faster response) and improve routing accuracy towards >= 90% target.
Output: Updated supervisor.py with 14+ new keyword patterns and 8+ new routing eval entries.
</objective>

<execution_context>
@C:/Users/tleub/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/tleub/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-agent-logic-hardening/04-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Expand supervisor keyword patterns and normalize logging</name>
  <files>documentologist-miran-service--neo4j/langgraph-agent/app/agents/supervisor.py</files>
  <read_first>
    - documentologist-miran-service--neo4j/langgraph-agent/app/agents/supervisor.py (full file — see current patterns and logging)
    - documentologist-miran-service--neo4j/langgraph-agent/tests/eval/dataset.json (eval entries to test against)
  </read_first>
  <action>
1. Add to `_VERIFY_KW` regex (inside the existing group, before the closing `)\b`):
   - `верно\s+ли` — factual compliance check
   - `допустим[оа]?\s+ли` — permissibility check
   - `имеет\s+ли\s+право` — rights verification
   - `вправе\s+ли` — authority check
   - `позволяет\s+ли\s+закон` — law permissibility
   - `можно\s+ли\s+(?!.*(?:получить|оформить|подать))` — legality (negative lookahead excludes procedural "как получить" queries which are search)

2. Add to `_SEARCH_KW` regex (inside the existing group, before the closing `)\b`):
   - `порядок\s+` — procedural queries
   - `условия\s+` — terms/conditions
   - `требования\s+(?:к|для|по)` — requirements queries
   - `ставка\s+` — rate queries
   - `обязанности\s+` — obligation info
   - `процедура\s+` — procedure queries
   - `страхование\s+` — insurance queries

3. Add to `_ANALYZE_KW` regex (inside the existing group, before the closing `)\b`):
   - `проанализируй` — direct analysis command

4. Normalize all three log lines in `classify_intent` to the same format. Replace:
   - Line ~171 (compound): change to `logger.info("supervisor: tier=compound intent=%s secondary=%s query=%r", primary, secondary, query[:60])`
   - Line ~192 (keyword): change to `logger.info("supervisor: tier=keyword intent=%s query=%r", kw_intent, query[:60])`
   - Line ~221 (llm): change to `logger.info("supervisor: tier=llm intent=%s query=%r raw=%r", intent, query[:60], raw[:40])`

IMPORTANT: When adding patterns to `_VERIFY_KW`, place the new entries in the "Legality checks" section after `допустимо\s+ли|правомерн`. When adding to `_SEARCH_KW`, add a new comment section `# Domain-specific informational` before the closing `)\b`. Test mentally that each new pattern does NOT conflict with higher-priority patterns (_INGEST_KW, _ANALYZE_KW checked first).
  </action>
  <verify>
    <automated>cd documentologist-miran-service--neo4j/langgraph-agent && python -c "
from app.agents.supervisor import _keyword_classify
tests = [
    ('верно ли что банк обязан иметь резервный фонд', 'verify'),
    ('допустимо ли включать такой пункт в договор', 'verify'),
    ('имеет ли право заёмщик досрочно погасить кредит', 'verify'),
    ('вправе ли банк повысить ставку', 'verify'),
    ('условия кредитования юридических лиц', 'search'),
    ('порядок погашения ипотечного кредита', 'search'),
    ('ставка рефинансирования ЦБ', 'search'),
    ('обязанности банка перед вкладчиками', 'search'),
    ('проанализируй этот документ', 'analyze'),
    ('расскажи про ипотеку', 'search'),
    ('загрузи документ', 'ingest'),
    ('составь договор', 'generate'),
]
failed = []
for q, expected in tests:
    result = _keyword_classify(q)
    if result != expected:
        failed.append(f'  FAIL: {q!r} -> {result} (expected {expected})')
if failed:
    print('FAILURES:')
    print('\n'.join(failed))
    exit(1)
print(f'All {len(tests)} routing tests passed')
"
    </automated>
  </verify>
  <acceptance_criteria>
    - grep -c "верно\\\\s+ли" supervisor.py returns 1
    - grep -c "допустим" supervisor.py returns at least 2 (existing допустимо + new допустим)
    - grep -c "имеет\\\\s+ли\\\\s+право" supervisor.py returns 1
    - grep -c "порядок\\\\s+" supervisor.py returns 1
    - grep -c "условия\\\\s+" supervisor.py returns 1
    - grep -c "проанализируй" supervisor.py returns 1
    - grep "supervisor: tier=" supervisor.py returns 3 lines (compound, keyword, llm)
    - No line contains the old format "Supervisor: compound" or "Supervisor: keyword intent=" or "Supervisor: llm intent="
  </acceptance_criteria>
  <done>14+ new keyword patterns added across _VERIFY_KW, _SEARCH_KW, _ANALYZE_KW. All 3 log lines normalized to "supervisor: tier=%s intent=%s" format. Existing eval queries still route correctly. New edge case queries route via keyword tier.</done>
</task>

<task type="auto">
  <name>Task 2: Add routing eval entries for new keyword patterns</name>
  <files>documentologist-miran-service--neo4j/langgraph-agent/tests/eval/dataset.json</files>
  <read_first>
    - documentologist-miran-service--neo4j/langgraph-agent/tests/eval/dataset.json (current entries — understand schema and IDs)
    - documentologist-miran-service--neo4j/langgraph-agent/app/agents/supervisor.py (verify patterns match new entries)
  </read_first>
  <action>
Add 8 new routing eval entries to dataset.json following the existing schema: `{"id": "route-NN", "query": "...", "expected_intent": "...", "category": "routing", "tags": ["routing", "keyword"]}`.

New entries (start numbering after last existing route-* entry):
1. `"верно ли что банк обязан иметь резервный фонд"` -> expected_intent: "verify"
2. `"допустимо ли включать штрафные санкции в договор ипотеки"` -> expected_intent: "verify"
3. `"имеет ли право заёмщик на досрочное погашение"` -> expected_intent: "verify"
4. `"вправе ли банк изменить процентную ставку в одностороннем порядке"` -> expected_intent: "verify"
5. `"условия кредитования юридических лиц"` -> expected_intent: "search"
6. `"порядок погашения ипотечного кредита"` -> expected_intent: "search"
7. `"обязанности банка перед вкладчиками"` -> expected_intent: "search"
8. `"проанализируй основные положения этого документа"` -> expected_intent: "analyze"

Each entry must have: id, query, expected_intent, category="routing", tags=["routing", "keyword"]. No expected_docs or expected_json_valid needed (routing-only entries).
  </action>
  <verify>
    <automated>cd documentologist-miran-service--neo4j/langgraph-agent && python -c "
import json
with open('tests/eval/dataset.json') as f:
    data = json.load(f)
routing = [e for e in data if e.get('category') == 'routing']
print(f'Total entries: {len(data)}')
print(f'Routing entries: {len(routing)}')
new_ids = [e['id'] for e in routing if int(e['id'].split('-')[1]) > 13]
print(f'New routing entries: {len(new_ids)}: {new_ids}')
assert len(new_ids) >= 8, f'Expected >= 8 new routing entries, got {len(new_ids)}'
for e in routing:
    assert 'expected_intent' in e, f'Missing expected_intent in {e[\"id\"]}'
    assert e['expected_intent'] in ('search','verify','analyze','generate','ingest'), f'Bad intent in {e[\"id\"]}'
print('All routing entries valid')
"
    </automated>
  </verify>
  <acceptance_criteria>
    - dataset.json contains at least 49 total entries (41 existing + 8 new)
    - At least 21 routing entries exist (13 existing + 8 new)
    - Each new entry has id matching pattern "route-\d+"
    - Each new entry has category "routing" and tags array containing "routing"
    - grep "верно ли" dataset.json returns at least 1 match
    - grep "допустимо ли" dataset.json returns at least 1 match
    - grep "условия кредитования" dataset.json returns at least 1 match
  </acceptance_criteria>
  <done>8 new routing eval entries added covering verify (4), search (3), and analyze (1) edge cases. Total eval dataset now has 49+ entries with 21+ routing tests. All new entries follow existing schema.</done>
</task>

</tasks>

<verification>
1. `cd documentologist-miran-service--neo4j/langgraph-agent && python -c "from app.agents.supervisor import _keyword_classify; print('import OK')"` -- supervisor imports without error
2. Keyword routing test (Task 1 verify command) passes all 12 cases
3. Dataset validation (Task 2 verify command) confirms 49+ entries, 21+ routing
4. No existing eval entries broken -- existing route-01..13 patterns still match
</verification>

<success_criteria>
- All 14+ new keyword patterns correctly route their target queries
- 3 supervisor log lines use normalized "supervisor: tier=%s intent=%s" format
- 8 new routing eval entries in dataset.json
- No regression: existing keyword patterns still work (ingest, analyze, generate, verify, search)
</success_criteria>

<output>
After completion, create `.planning/phases/04-agent-logic-hardening/04-01-SUMMARY.md`
</output>
