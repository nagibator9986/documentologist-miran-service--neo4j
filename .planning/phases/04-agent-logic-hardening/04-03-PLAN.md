---
phase: 04-agent-logic-hardening
plan: 03
type: execute
wave: 2
depends_on: ["04-01", "04-02"]
files_modified:
  - documentologist-miran-service--neo4j/langgraph-agent/app/agents/search_agent.py
  - documentologist-miran-service--neo4j/langgraph-agent/app/agents/analyze_agent.py
  - documentologist-miran-service--neo4j/langgraph-agent/app/api/v1/chat.py
  - documentologist-miran-service--neo4j/langgraph-agent/app/api/v1/completions.py
autonomous: true
requirements: [P4-HALLUC, P4-DEGRADE]

must_haves:
  truths:
    - "When search_agent has context but LLM returns a 'no info' stub, it retries once with explicit instruction"
    - "When LLM returns empty string (Ollama down), search_agent returns a graceful message instead of empty response"
    - "When LLM returns empty string, analyze_agent text tasks return a graceful message instead of empty result"
    - "HTTP 500 from graph execution is converted to 200 with error message in chat.py and completions.py"
    - "Memory agent Redis failure is confirmed non-fatal (already implemented)"
  artifacts:
    - path: "documentologist-miran-service--neo4j/langgraph-agent/app/agents/search_agent.py"
      provides: "Hallucination guard + empty answer guard"
      contains: "_HALLUCINATION_STUBS"
    - path: "documentologist-miran-service--neo4j/langgraph-agent/app/agents/analyze_agent.py"
      provides: "Empty LLM response guard for text tasks"
      contains: "_OLLAMA_UNAVAILABLE_MSG"
    - path: "documentologist-miran-service--neo4j/langgraph-agent/app/api/v1/chat.py"
      provides: "Graceful 200 response instead of 500"
    - path: "documentologist-miran-service--neo4j/langgraph-agent/app/api/v1/completions.py"
      provides: "Graceful 200 response instead of 500"
  key_links:
    - from: "search_agent._generate_answer return"
      to: "hallucination guard check"
      via: "regex match on answer when has_context=True"
      pattern: "_HALLUCINATION_STUBS\\.search"
    - from: "search_agent.search_node answer"
      to: "_OLLAMA_UNAVAILABLE_MSG"
      via: "empty string check"
      pattern: "not answer\\.strip\\(\\)"
    - from: "chat.py Exception handler"
      to: "graceful 200 response"
      via: "return ChatResponse instead of HTTPException(500)"
      pattern: "ChatResponse.*degraded"
---

<objective>
Add hallucination detection in search_agent (retry when LLM claims "no info" despite having context), add empty-response guards for Ollama failures across search and analyze agents, and convert HTTP 500 errors to graceful 200 responses in the API layer.

Purpose: Users get meaningful responses even when Ollama is slow/down, and false "no information" answers are caught and retried.
Output: Updated search_agent, analyze_agent, chat.py, completions.py with graceful degradation.
</objective>

<execution_context>
@C:/Users/tleub/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/tleub/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-agent-logic-hardening/04-RESEARCH.md
@.planning/phases/04-agent-logic-hardening/04-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add hallucination guard and empty-answer guard to search_agent</name>
  <files>documentologist-miran-service--neo4j/langgraph-agent/app/agents/search_agent.py</files>
  <read_first>
    - documentologist-miran-service--neo4j/langgraph-agent/app/agents/search_agent.py (full file — see search_node, _generate_answer, _build_llm_prompt, has_context usage, build_final_response)
    - documentologist-miran-service--neo4j/langgraph-agent/app/prompts/__init__.py (see SEARCH_EXPERT prompt for retry context)
  </read_first>
  <action>
1. **Add hallucination stub regex** near the top of the file (after imports, before pipeline functions):
```python
_HALLUCINATION_STUBS = re.compile(
    r"(?:у меня нет (?:информации|данных)"
    r"|не располагаю информацией"
    r"|не (?:могу|удалось) найти (?:информацию|данные|ответ)"
    r"|в (?:предоставленных|загруженных) документах не найдено"
    r"|в базе знаний не найдено"
    r"|к сожалению.{0,30}(?:нет информации|не найдено))",
    re.IGNORECASE | re.UNICODE,
)
```

2. **Add graceful degradation message** near the same location:
```python
_OLLAMA_UNAVAILABLE_MSG = (
    "К сожалению, языковая модель временно недоступна. "
    "Найденные документы доступны в цитатах ниже. "
    "Попробуйте повторить запрос через несколько минут."
)
```

3. **Add hallucination guard + empty guard in `search_node`** after line 605 (`answer = _generate_answer(user_msg, state, s)`). Insert between `answer = _generate_answer(...)` and `citations = _build_citations(...)`:

```python
    # Hallucination guard: retry once if LLM stubs despite having context
    if has_context and answer.strip() and _HALLUCINATION_STUBS.search(answer):
        logger.warning(
            "search_node: hallucination stub detected with has_context=True, retrying"
        )
        retry_prompt = (
            f"{user_msg}\n\n"
            "ВАЖНО: Ответь на основе предоставленного контекста. "
            "Документы содержат релевантную информацию — используй её."
        )
        answer = _generate_answer(retry_prompt, state, s)

    # Empty answer guard: Ollama may be down (invoke_with_retry returns "")
    if not answer.strip():
        logger.error("search_node: LLM returned empty response (Ollama may be down)")
        answer = _OLLAMA_UNAVAILABLE_MSG
```

Also add `"degraded": True` to the metrics dict when either guard fires. After the metrics dict is built (~line 614-633), add:
```python
    if answer == _OLLAMA_UNAVAILABLE_MSG:
        metrics["degraded"] = True
```

IMPORTANT: The hallucination guard ONLY fires when `has_context=True` AND the answer is non-empty AND matches the stub pattern. It retries ONCE. If the retry also stubs, accept it. The empty guard fires AFTER the hallucination guard, so if retry returns empty, it gets the graceful message.
  </action>
  <verify>
    <automated>cd documentologist-miran-service--neo4j/langgraph-agent && python -c "
import re
# Verify the regex compiles and matches expected patterns
_HALLUCINATION_STUBS = re.compile(
    r'(?:у меня нет (?:информации|данных)'
    r'|не располагаю информацией'
    r'|не (?:могу|удалось) найти (?:информацию|данные|ответ)'
    r'|в (?:предоставленных|загруженных) документах не найдено'
    r'|в базе знаний не найдено'
    r'|к сожалению.{0,30}(?:нет информации|не найдено))',
    re.IGNORECASE | re.UNICODE,
)
tests = [
    ('У меня нет информации по данному вопросу', True),
    ('Не могу найти информацию в предоставленных документах', True),
    ('К сожалению, в базе нет информации', True),
    ('В предоставленных документах не найдено ответа', True),
    ('Согласно статье 5 закона, банк обязан...', False),
    ('Информация о кредитных ставках представлена ниже', False),
]
for text, should_match in tests:
    matched = bool(_HALLUCINATION_STUBS.search(text))
    status = 'OK' if matched == should_match else 'FAIL'
    print(f'{status}: {text[:50]!r} -> matched={matched} (expected={should_match})')
    assert matched == should_match, f'Pattern mismatch for {text!r}'
print('All hallucination pattern tests passed')

# Verify module imports
from app.agents.search_agent import search_node
print('search_agent imports OK')
"
    </automated>
  </verify>
  <acceptance_criteria>
    - grep "_HALLUCINATION_STUBS" search_agent.py returns at least 2 matches (definition + usage)
    - grep "_OLLAMA_UNAVAILABLE_MSG" search_agent.py returns at least 2 matches (definition + usage)
    - grep "degraded" search_agent.py returns at least 1 match
    - grep "hallucination stub detected" search_agent.py returns 1 match
    - grep "LLM returned empty response" search_agent.py returns 1 match
    - The hallucination guard is between _generate_answer call and _build_citations call in search_node
    - Python import of search_agent succeeds
  </acceptance_criteria>
  <done>Hallucination guard retries once when LLM stubs despite having context. Empty answer guard provides graceful message when Ollama is down. Both guards log at appropriate levels (WARNING for hallucination, ERROR for empty). Degraded flag added to metrics.</done>
</task>

<task type="auto">
  <name>Task 2: Add empty-response guard to analyze_agent and graceful degradation to HTTP layer</name>
  <files>
    documentologist-miran-service--neo4j/langgraph-agent/app/agents/analyze_agent.py
    documentologist-miran-service--neo4j/langgraph-agent/app/api/v1/chat.py
    documentologist-miran-service--neo4j/langgraph-agent/app/api/v1/completions.py
    documentologist-miran-service--neo4j/langgraph-agent/app/agents/memory_agent.py
  </files>
  <read_first>
    - documentologist-miran-service--neo4j/langgraph-agent/app/agents/analyze_agent.py (see _run_analysis_llm for text tasks, _parse_result)
    - documentologist-miran-service--neo4j/langgraph-agent/app/api/v1/chat.py (see Exception handler in chat endpoint ~line 248-250, ChatResponse model)
    - documentologist-miran-service--neo4j/langgraph-agent/app/api/v1/completions.py (see Exception handler ~line 244-246)
    - documentologist-miran-service--neo4j/langgraph-agent/app/agents/memory_agent.py (verify Redis failure is non-fatal — confirm try/except in memory_save_node and memory_load_node)
  </read_first>
  <action>
1. **analyze_agent.py — Add graceful message for empty LLM response in text tasks:**

Add the same `_OLLAMA_UNAVAILABLE_MSG` constant near the top (after imports):
```python
_OLLAMA_UNAVAILABLE_MSG = (
    "К сожалению, языковая модель временно недоступна. "
    "Попробуйте повторить запрос через несколько минут."
)
```

In `_run_analysis_llm` (the text task path, where `invoke_with_retry` is called for qa/summary), after the LLM call returns, check for empty response. Find the line where `raw` is assigned from `invoke_with_retry` for text tasks. If `raw` is empty string, replace with `_OLLAMA_UNAVAILABLE_MSG` and log ERROR:
```python
    if not raw.strip():
        logger.error("analyze: LLM returned empty for task=%s (Ollama may be down)", task)
        raw = _OLLAMA_UNAVAILABLE_MSG
```

2. **chat.py — Convert 500 to graceful 200 in REST endpoint:**

In the `chat` endpoint function (~line 248), replace:
```python
    except Exception as exc:
        logger.exception("Graph execution failed")
        raise HTTPException(status_code=500, detail=str(exc)) from exc
```
With:
```python
    except Exception as exc:
        logger.exception("Graph execution failed")
        return ChatResponse(
            session_id=req.session_id,
            intent="search",
            intents=["search"],
            response="К сожалению, произошла внутренняя ошибка. Попробуйте повторить запрос.",
            citations=[],
            export_path=None,
            verify_result=None,
            generate_result=None,
            analyze_result=None,
            retrieval_metrics={"degraded": True, "error": str(exc)},
        )
```

3. **completions.py — Convert 500 to graceful 200:**

In the non-streaming completions endpoint (~line 244), replace:
```python
    except Exception as exc:
        logger.exception("Graph error in completions (non-streaming)")
        raise HTTPException(status_code=500, detail=str(exc)) from exc
```
With:
```python
    except Exception as exc:
        logger.exception("Graph error in completions (non-streaming)")
        error_msg = "К сожалению, произошла внутренняя ошибка. Попробуйте повторить запрос."
        return JSONResponse({
            "id": _make_id(),
            "object": "chat.completion",
            "created": int(time.time()),
            "model": req.model,
            "choices": [{
                "index": 0,
                "message": {"role": "assistant", "content": error_msg},
                "finish_reason": "error",
            }],
            "usage": {"prompt_tokens": 0, "completion_tokens": 0, "total_tokens": 0},
        })
```

4. **memory_agent.py — Verify Redis non-fatal (READ ONLY, no changes expected):**

Read `memory_save_node` and `memory_load_node`. Confirm both wrap Redis operations in try/except with WARNING log. If already non-fatal (as research indicates), note confirmation in summary. If there is a gap, fix it with the same try/except + WARNING pattern.

The research confirms memory_agent is already non-fatal. This is a verification task — if code matches expectations, no changes needed to memory_agent.py.
  </action>
  <verify>
    <automated>cd documentologist-miran-service--neo4j/langgraph-agent && python -c "
# Verify analyze_agent imports and has the guard
from app.agents.analyze_agent import analyze_node
print('analyze_agent imports OK')

# Verify chat.py no longer raises 500 for generic Exception
import ast, inspect
# Check chat module loads
from app.api.v1 import chat
print('chat module imports OK')

# Verify completions module loads
from app.api.v1 import completions
print('completions module imports OK')

# Verify memory_agent non-fatal pattern
import inspect
from app.agents.memory_agent import memory_save_node, memory_load_node
save_src = inspect.getsource(memory_save_node)
load_src = inspect.getsource(memory_load_node)
assert 'except' in save_src, 'memory_save_node missing exception handler'
assert 'except' in load_src, 'memory_load_node missing exception handler'
assert 'WARNING' in save_src or 'warning' in save_src, 'memory_save_node missing warning log'
print('memory_agent Redis failure is non-fatal (confirmed)')
print('ALL CHECKS PASSED')
"
    </automated>
  </verify>
  <acceptance_criteria>
    - grep "_OLLAMA_UNAVAILABLE_MSG" analyze_agent.py returns at least 2 matches
    - grep "degraded" chat.py returns at least 1 match
    - grep "HTTPException.*500" chat.py returns 0 matches in the chat endpoint function (the 504 timeout one should remain)
    - grep "HTTPException.*500" completions.py returns 0 matches in the non-streaming endpoint
    - grep "finish_reason.*error" completions.py returns 1 match
    - Python import of all four modules succeeds without error
    - memory_agent.py contains try/except in both memory_save_node and memory_load_node (confirmed, not modified)
  </acceptance_criteria>
  <done>Graceful degradation complete: analyze_agent handles empty LLM responses for text tasks, chat.py returns 200 with error message instead of 500, completions.py returns valid OpenAI-format response with finish_reason="error", memory_agent Redis failure confirmed non-fatal.</done>
</task>

</tasks>

<verification>
1. All four modules import cleanly: search_agent, analyze_agent, chat, completions
2. `grep -c "degraded" documentologist-miran-service--neo4j/langgraph-agent/app/agents/search_agent.py` >= 1
3. `grep -c "_OLLAMA_UNAVAILABLE_MSG" documentologist-miran-service--neo4j/langgraph-agent/app/agents/analyze_agent.py` >= 2
4. `grep -c "HTTPException.*500" documentologist-miran-service--neo4j/langgraph-agent/app/api/v1/chat.py` == 0 (in chat endpoint)
5. No 500 HTTPException in non-streaming completions endpoint
6. memory_agent.py try/except pattern verified in both nodes
</verification>

<success_criteria>
- Search agent detects hallucination stubs and retries once when has_context=True
- Empty LLM responses produce graceful user-facing messages in search and analyze agents
- HTTP layer returns 200 with error message instead of 500 for graph execution failures
- Memory agent Redis failure confirmed non-fatal (both load and save)
- All degraded responses include appropriate ERROR-level logging
</success_criteria>

<output>
After completion, create `.planning/phases/04-agent-logic-hardening/04-03-SUMMARY.md`
</output>
